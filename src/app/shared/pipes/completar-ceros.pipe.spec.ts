import { CompletarCerosPipe } from './completar-ceros.pipe';

describe('CompletarCerosPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletarCerosPipe();
    expect(pipe).toBeTruthy();
  });
});
