import { ValidaSecuenciaDirective } from './valida-secuencia.directive';

describe('ValidaSecuenciaDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaSecuenciaDirective();
    expect(directive).toBeTruthy();
  });
});
